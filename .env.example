# =====================================================
# SERVER CONFIGURATION
# =====================================================

# Puerto en el que correrá el servidor
PORT=3000

# Ambiente de ejecución (development, production, test)
NODE_ENV=development

# URL de la API (usado para callbacks de OAuth)
API_URL=http://localhost:3000

# URL del frontend (usado para redirecciones y emails)
FRONTEND_URL=http://localhost:3001

# =====================================================
# DATABASE
# =====================================================

# URL de conexión a PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_bd?schema=public
DATABASE_URL="postgresql://postgres:password@localhost:5432/university_organizer?schema=public"

# =====================================================
# JWT & AUTHENTICATION
# =====================================================

# Secreto para firmar tokens JWT (CAMBIAR EN PRODUCCIÓN)
# Generar con: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Secreto para refresh tokens (CAMBIAR EN PRODUCCIÓN)
# Generar con: openssl rand -base64 32
JWT_REFRESH_SECRET=your-super-secret-refresh-jwt-key-change-this-in-production

# Emisor de los tokens JWT
JWT_ISSUER=university-organizer

# Tiempo de expiración del access token (en segundos)
# 900 = 15 minutos
TOKEN_EXPIRATION=900

# Secreto para las sesiones (CAMBIAR EN PRODUCCIÓN)
# Generar con: openssl rand -base64 32
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# =====================================================
# GOOGLE OAUTH (Opcional)
# =====================================================

# Obtener en: https://console.cloud.google.com/
# 1. Crear proyecto
# 2. Activar Google+ API
# 3. Crear credenciales OAuth 2.0
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =====================================================
# EMAIL CONFIGURATION (SMTP)
# =====================================================

# Configuración del servidor SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Credenciales de email
# Para Gmail: Usar "App Password" en lugar de la contraseña normal
# 1. Activar verificación en 2 pasos
# 2. Ir a: https://myaccount.google.com/apppasswords
# 3. Generar contraseña de aplicación
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password

# Email que aparece como remitente
EMAIL_FROM=noreply@university-organizer.com
EMAIL_FROM_NAME=University Organizer

# =====================================================
# NOTAS IMPORTANTES
# =====================================================

# 1. NUNCA subir este archivo con valores reales a Git
# 2. En producción, usar variables de entorno del sistema o servicios como:
#    - Vercel/Netlify: Variables de entorno en el dashboard
#    - Docker: Variables en docker-compose.yml o Dockerfile
#    - Heroku: heroku config:set
# 3. Generar secretos seguros para JWT y SESSION_SECRET
# 4. Para Gmail, SIEMPRE usar App Passwords, no la contraseña real
# 5. DATABASE_URL debe apuntar a tu base de datos PostgreSQL
